<h1>Carrito</h1>

{{#if productsCart.length}}
  <ul>
    {{#each productsCart}}
      <li>
        <h2>{{this.name}}</h2>
        <h2>{{this._id}}</h2>
        <p>Cantidad: {{this.quantity}}</p>
        <!-- Puedes mostrar más detalles del producto aquí, como nombre, precio, etc., utilizando la estructura del objeto del producto -->
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No hay productos en el carrito</p>
{{/if}}
